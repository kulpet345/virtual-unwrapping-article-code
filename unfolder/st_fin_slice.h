#include "utility.h"

std::vector<int> semi_automatic_find_st_fin(std::vector<std::string> mk_paths, bool st, const std::vector<int>& nums);

int automatic_find_st_fin(std::vector<std::string> mk_paths, bool st, const std::vector<int>& nums, int slice_count);